# 設計書：期間對齊、缺值呈現、CF 科目補齊（2026-08-22）

**專案**：SEC Financial Fetcher
**用途**：這份是**動手前的完整規格**，給接手的 agent 照著做。CTH 已逐項確認方向，
下面每一項都寫了「為什麼」「規格」「動哪些檔案」「測試要釘什麼」「怎麼驗收」「風險」。
**沒有寫在這裡的東西不要順手做**，發現新問題就記進 `docs/TODO.md` 再問 CTH。

前置閱讀：`docs/superpowers/report-2026-08-22-quarter-label-and-alignment.md`
（前一輪已完成的修復與當時的決策記錄）。

---

## 0. 執行順序與相依

```
G1  →  G3  →  G2 + G7  →  G6  →  G8
```

| 相依 | 原因 |
|---|---|
| **G6 必須等 G1** | G6 會產生「第 5 列不是完整 ISO 日期」的欄位，那正是 G1 要收掉的殘留錯值路徑。順序反過來會直接生出一堆錯的日曆季 |
| **G2 與 G7 綁一起** | 都改 `comparison_writer.py` 的版面，分兩次會動兩遍同一份檔案、測試也要跑兩遍 |
| **G8 放最後** | 它會改變所有既有期間的資料來源優先序，前面幾項的驗收基準會跟著漂。等前面穩定再做 |

**不要開平行 subagent。** G2/G6/G7 都改 `comparison_writer.py`；而且每項都要跑全套測試
（含真連 SEC 的 live 測試），實測兩份測試併發會互搶 SEC 頻寬，20 分鐘變 40 分鐘。
單一 worker 循序做最快。

**每一項的收尾都一樣**：跑全套 `venv/Scripts/python.exe -m pytest tests/ -q`
（約 15–30 分鐘，會真的連 SEC），然後更新 `docs/CHANGELOG.md` 與 `docs/TODO.md`。
不要只跑相關的測試檔就宣稱完成。

---

## G1. 日曆季判準統一成「一套演算法、兩個基準點」

### 現況：同一件事有三套算法

| # | 位置 | 判準 | INTC 季末 2023-04-01 | NVDA 季末 2025-07-27 |
|---|---|---|---|---|
| 1 | `fetcher_gaap._calendar_quarter()`（`fetcher_gaap.py:1502`） | 期末日字面落點，**完全不內縮** | `2023Q2` ❌ | `2025Q3` |
| 2 | `fiscal_input.calendar_quarter_of()` + `calendar_quarter_formula()` | 期末日 −15 天 | `2023Q1` | `2025Q3` |
| 3 | `fiscal_input.calendarized_quarter_of()` | 期中點 −45 天 | `2023Q1` | `2025Q2` |

#1 是純 bug：INTC 那一季實際涵蓋 1–3 月，說它是 Q2 錯得離譜。平常被 #2 的公式蓋掉看不到，
但**第 5 列不是完整 ISO 日期的欄位**（合成 Q4 的年報期末日有時只有 `2010-01`）
`fiscal_input._apply_to_sheet()` 會跳過公式，那一格就保留 #1 寫的錯值。

### CTH 決策

- 「實際結束日」採**後者的讀法**＝**期末日去掉 52/53 週漂移**（也就是現行的 −15），
  不是字面期末日。→ **第 4 列的公式不用改**
- 三套要統一

### 規格

**不是把三套合併成一個數字**——#2 與 #3 回答的是不同問題，強行合一必然弄壞其中一邊：

- 全改 −15 → 跨公司對齊壞掉，NVDA 整條線偏一格
- 全改 −45 → 第 4 列會對一個 7 月結束的季說「2025Q2」，跟正下方第 5 列的 `2025-07-27` 自相矛盾

**統一的做法是「一份實作、兩個具名基準點、呼叫端必須明講要哪一個」**：

1. **刪掉 #1**。`fetcher_gaap._calendar_quarter()` 改成呼叫 `fiscal_input` 的實作，
   不要自己算。它原本的「期末日抓不到就用財季標籤反推」那條退路要保留
   （`_fiscal_period_end()`），只是算法換掉。
   - 注意循環匯入：`fiscal_input → excel_formatter → fetcher_gaap`。比照
     `_col_to_quarter_label()` 現在的寫法用函式內延後 import。
2. **#2 與 #3 合併成同一個函式，用參數區分基準點**：

   ```python
   # fiscal_input.py
   _BASIS_DAYS = {"end": 15, "span": 45}   # 名稱是規格的一部分，不要改成數字參數

   def calendar_quarter_of(period_end, *, basis):
       """期末日 → 日曆季。basis 沒有預設值，呼叫端一定要明講。

       basis="end"  結算季：這一季**結束**在哪個日曆季（內縮 15 天吃掉 52/53 週漂移）
       basis="span" 對齊季：這一季的**多數天數**落在哪個日曆季（期中點）
       """
   ```
   - **`basis` 不給預設值**，強迫每個呼叫端表態。這就是「統一」的實質：
     以後只有一個地方會算錯，而且每個使用點都看得出它要的是哪種語意
   - `calendarized_quarter_of()` / `calendarized_year_of()` 保留成薄包裝
     （`basis="span"`），不要讓 `comparison.py` 直接傳字串參數
3. **Excel 公式維持 `basis="end"` 的邏輯**（`_date_expr()` 的 −15 不變）。
   公式是 Python 那份的規格複寫，兩邊都要在註解裡互相指到對方。
4. **顯示上要分得出來**。真正讓人混淆的不是「有兩套」，是兩邊都印成一模一樣的 `2025Q2`。
   - 單一公司第 4 列：**機器鍵 `Calendar Quarter` 不動**（改了會打斷下游腳本，見
     `docs/TODO.md` D0-5 的教訓），只改 B 欄譯文，講明是「期末日所在季」
   - 跨公司：欄位標題維持純 `2025Q2`（它是圖表的 X 軸類別，加前綴會很醜），
     語意寫在 G7 的說明 sheet 裡

### 動到的檔案

- `src/fiscal_input.py`（合併函式、`_BASIS_DAYS`、docstring）
- `src/fetcher_gaap.py:1502` `_calendar_quarter()`
- `src/comparison.py`（如果薄包裝改名）
- `src/locales/*.py` 四個語系檔：第 4 列 B 欄譯文（**四個都要改，不可只改繁中**）

### 測試要釘什麼

- `calendar_quarter_of()` 不給 `basis` 要噴 `TypeError`（釘住「呼叫端必須表態」）
- 同一個期末日在兩種 basis 下的差異：`2025-07-27` → `end`=`2025Q3`、`span`=`2025Q2`
- `fetcher_gaap._calendar_quarter()` 對 `2023-04-01` 要回 `2023Q1`（釘住 #1 被修掉）
- 期末日抓不到時 `_calendar_quarter()` 仍走財季標籤反推的退路

### 驗收

重跑 NVDA 單一公司，確認第 4 列在**所有欄位**（含合成 Q4 那些殘留格）都是同一套算法的結果。
方法：讀 xlsx 時同時看第 4 列與第 5 列，自己用 `basis="end"` 算一遍逐格比對。

### 風險：低

---

## G3. IS 區的 SBC / D&A 只有 Q1 有值

### 現況與根因（已實測定位，不用重查）

`output/_verify/NVDA_v4.xlsx` 的 `Data_Financials(Q)`：

```
IS 區  第 13 列  D&A (CF memo)   缺 51/68
IS 區  第 27 列  SBC             缺 51/68
CF 區  第 89 列  D&A             缺  1/68   ← 好的
CF 區  第 90 列  SBC             缺  1/68   ← 好的
```

缺的期間有規律：**Q1 都有值，Q2/Q3 全空**（2010-08-01／2010-10-31／2011-07-31／
2011-10-30…），再加上 17 個合成 Q4。

根因：`IS_TEMPLATE` 裡 `source == "CF"` 的兩列在 `_build_is_table()` 走
`cf_q_col = _current_q_col(cf_df)`（`fetcher_gaap.py:877`）——直接找現金流量表的**單季欄**。
但 10-Q 的現金流量表是 **YTD 累計**，Q2/Q3 的 filing 根本沒有單季欄，`_current_q_col()`
回 `None`，那兩列就拿不到值。

`_build_cf_table()`（`fetcher_gaap.py:1275-1290`、`1365-1382`）另外做了 YTD 拆算：
找不到單季欄時改用 `_ytd_col()`，標記 `is_ytd=True`，最後用「本季 YTD − 上季 YTD」
還原單季。**IS 這條路完全沒走這段。**

連帶：Q2/Q3 是 `None`，`_synthesize_q4()` 的「年報 − Q1 − Q2 − Q3」也算不出來。

### 規格

**不要在 `_build_is_table()` 裡重寫一份 YTD 拆算**（那就是第二份會漂移的實作）。
改成 **IS 的 CF-sourced 列從已經建好的 `cf_tbl` 依 label 回填**：

1. `_build_is_table()` 拿掉 CF-sourced 列自己讀 `cf_df` 的那段，那兩列先留 `None`
2. 在 `fetch_gaap_statements()` 裡，**`_synthesize_q4()` 跑完之後**
   （`fetcher_gaap.py:2207-2209` 附近），用 `cf_tbl` 回填 `is_tbl`：

   | IS 列名 | 來源 CF 列名 |
   |---|---|
   | `SBC` | `SBC` |
   | `D&A (CF memo)` | `D&A` |

   依 `quarter_labels` 對照，CF 有值才寫、沒有就維持 `None`
3. 放在 Q4 合成之後，IS 那兩列就連合成 Q4 都跟 CF 一致（CF 區實測只缺 1 期）

### 要注意的坑

- **override 會被繞過**：現行 `_build_is_table()` 對這兩列會先套 `is_overrides`。
  改成回填之後，IS 側的 override 就沒有作用了。查一下 `override_engine.KEY_ROWS`
  ——`SBC`／`D&A` 都不在 KEY_ROWS 裡，理論上不會有 override，但**動手前要實際
  確認 `overrides.get("IS", {})` 不會帶到這兩個 row_name**，有的話要決定怎麼處理
- 年報路徑不受影響：10-K 的現金流量表有 `(FY)` 欄，`_is_q_col()` 對 `FY` 回 True，
  `_current_q_col()` 找得到，所以年報那兩列本來就是好的
- `_build_is_table()` 裡 `consumed` 只追蹤 IS df 的 index，CF-sourced 列本來就不進去，
  拿掉那段不影響 overflow 計算

### 測試要釘什麼

- 用假 filing 建一組「Q1 有單季欄、Q2/Q3 只有 YTD 欄」的資料，跑
  `fetch_gaap_statements()`，斷言 IS 的 `SBC` 在 Q2/Q3 有值且等於 CF 的 `SBC`
- 斷言 IS 的 `SBC`／`D&A (CF memo)` 與 CF 的 `SBC`／`D&A` **逐格相等**（含合成 Q4）

### 驗收

重跑 NVDA，`Data_Financials(Q)` 的第 13/27 列缺漏數要從 51 降到跟第 89/90 列一樣（1）。

### 風險：中（碰 `fetch_gaap_statements()` 的組裝流程）

---

## G2 + G7. 跨公司輸出改版

兩項一起做，同一次改 `comparison_writer.py`。

### G2. `Compare_Data` 最上方加「日曆季 ↔ 財季」對應表

CTH 確認的版面（比原提案多帶期末日，一格講完）：

```
             2024Q1              2024Q2              2024Q3        ...
AMD          FY2024Q1 (0330)     FY2024Q2 (0629)     FY2024Q3 (0928)
NVDA         FY2025Q1 (0428)     FY2025Q2 (0728)     FY2025Q3 (1027)
AVGO         FY2024Q2 (0505)     FY2024Q3 (0804)     FY2024Q4 (1103)
```

- 放在整張 `Compare_Data` 的**最上方**，在第一個指標區塊之前
- 下面的財務指標區塊**只給日曆季**，不重複財季
- **為什麼用對應表而不是「只寫每家公司的財年開始月份」**：對應表是逐期從實際期末日
  算出來的，公司哪一年改過財年，那一欄自己就會反映出來，**不需要任何例外處理**。
  寫「財年開始月份」只能寫一個值，公司改過財年就直接失效

**前置改動**：`comparison.ComparisonResult` 目前只留 `period_ends`，
`_aligned_labels()` 轉換完就把原本的財季標籤丟了。要多帶一個欄位：

```python
fiscal_labels: dict[str, dict[str, str]]   # {ticker: {日曆季: 財季}}
```

`build_comparison()` 在跑 `_aligned_labels()` 時順手把 `(aligned, 原label)` 存起來。

**注意**：`write_chart_sheets()` 用 `block_ranges` 定位資料列、用 `data_ws.max_column`
定寬度。最上方插入區塊會把所有列號往下推——`block_ranges` 是回傳值所以會自己跟著走，
但 `write_snapshot_sheets()` 也吃 `block_ranges`，要一起確認。**這裡要有測試釘住
「插入對應表之後，Snapshot 公式仍指到正確的列」**。

### G7. 加一張「說明」sheet（2026-08-22 CTH 追加 checkbox 需求）

CTH 明確說這張表未來會擴充，所以**要做成資料驅動**——一個
`list of (標題鍵, 內文鍵, 判定函式)`，新增一條只要加一行 + 四個 locale 各加兩條，
不可把文字寫死在版面程式裡。

**加上「本檔是否適用」的勾選欄**（CTH 要求）：大多數條目可以用實際資料算出來，
勾起來代表「這份檔案真的踩到這個狀況」。使用者不必讀完九條去猜哪條跟他有關。

版面：

| ✓ | 說明項目 | 內容 | 這份檔案的實際情況 |
|---|---|---|---|
| ✓ | 各公司財年結束月不同 | … | NVDA 1 月、AVGO 11 月、其餘 12 月 |
| ✓ | 同一欄各公司期末日不同 | … | 最大差 29 天（2025Q2） |
|   | Q4 為推算值 | … | （本檔無推算 Q4） |

**判定方式：**

| 條目 | 判定 |
|---|---|
| 時間軸＝日曆季、判準期中點 | 結構性，永遠勾 |
| 各公司財年結束月不同 | 由各公司期末日推出的結算月不只一種 |
| 同欄各公司期末日不同 | 任一欄的 `period_ends` 各公司不全等 |
| 期末結算日取最晚 | 同上（有差異才需要說明取哪個） |
| Q4 為推算值 | **需要 `comparison` 多帶一個旗標，目前沒有，要加** |
| 有缺值 | 任一格是 `None` |
| 期間早於 XBRL 強制申報 | 最早一欄的期末日早於 2011 |
| 單位、定義適用範圍 | 結構性，永遠勾 |

**必須新增的三條（原本九條沒有）：**

1. **抓取失敗的公司** ← 最重要。現在失敗只寫進 GUI log，**檔案裡完全看不到**。
   `INTC_NVDA_AMD_TSM_v3.xlsx` 檔名有 TSM 但裡面根本沒有 TSM（外國發行人報
   20-F），從檔案完全看不出來。要列出失敗 ticker 與原因（`result.failures`）
2. **符號照公司原始申報，未做正規化**（CTH 2026-08-22 決定）。同一科目在不同
   公司、甚至同一公司不同年度可能正負不同，使用者自行判讀
3. **數字是「當初申報值」還是「含後續重編」**——實測 5.4% 的格子兩者不同

**已查出一條原本寫錯的：** 草稿第 5 條「Q4 是推算的，`Other (as reported)` 區
不補」——**後半句對這份檔案是錯的**。`main.py:1749` 顯示跨公司可選指標只來自
`IS/BS/CF_TEMPLATE` + `RATIO_DEFS`，overflow 列根本不在選單裡，寫了只會讓人困惑。
**要刪掉後半句。** 其餘八條已逐條對照程式確認正確。

### 測試要釘什麼

- 對應表：多家不同財年結束月的公司，斷言同一個日曆季欄位下各家財季不同且正確
- 對應表：公司中途改財年的情境（假資料即可），斷言那一欄自己就對
- Snapshot 公式在插入對應表之後仍指到正確的列（**這是最容易壞的地方**）
- 說明 sheet：條目數量、四個 locale 都查得到 key（比照現有的 `test_i18n.py` 寫法）

### 驗收

用 AMD / NVDA / AVGO / INTC / MRVL / LITE 六家（財年結束月分別是 12／1／11／12／1／6-7 月）
真實資料產一份，肉眼確認對應表每一格都對得上該公司財報上寫的財季。

### 風險：中（動版面，Snapshot 公式的列號是主要風險點）

---

## G6. 抓不到的季度不要跳過，留一整欄空白

### CTH 的要求

現況欄位清單是「成功抓到什麼就放什麼」，某一季掛掉整欄消失，畫面上 25Q3 直接跳到 26Q1，
使用者與 AI 都看不出中間漏了一季。要改成**保留欄位、內容全空**，讓「有漏」看得見。

### 規格（2026-08-22 用 52 家實際資料定案）

**判定公式就是一行：**

```python
missing = round((next_period_end - this_period_end).days / _QUARTER_DAYS) - 1
```

`_QUARTER_DAYS = 91`（13 週 × 7 天，**要寫成具名常數並註明來源**，不是隨手取的數字）。

**用 52 家、1,482 對相鄰期間驗證過：**

```
<70 天                    1   0.1%
70-110（正常一季）      1413  95.3%
111-150                  16   1.1%
151-210（缺 1 季）        52   3.5%
211-300                   0   0.0%
>300                      0   0.0%
```

- 111~150 那 16 筆**全部是 COSTCO**——它的第四季是 16 週（112~119 天）。
  `round(112/91) = 1`，正確判為「沒缺」。**這就是為什麼不能用固定門檻（例如
  「>120 天算缺」），要用 round 除法**
- 151~210 那 52 筆都落在 182~189 天，`round(182/91) = 2` → 補 1 欄
- **實測沒有任何 >210 天的案例**，所以實務上不會連缺兩季
- 那 1 筆 <70 天是 SNOW 的 `2022-01-31 → 2022-01-31`（0 天，同一個期末日出現
  兩次）。`round(0/91) = 0`，規則自然不補——但那是**重複列**，要另開條目查

**配套：**

- **單一缺口最多補 4 欄**。實測沒有 >210 天的，真出現就是資料異常，
  不該讓程式無限生欄
- 只補在「最早抓到的那一季」與「最新」之間（CTH 已定）
- 缺欄的第 5 列期末日用 `_fiscal_period_end()` 反推年月

### 風險：中

---

## G8. 用「比較欄」當 fallback 補洞

### 背景（實測，不是推測）

CTH 問「為什麼 2009Q4 都沒資料」。直接原因：2009Q4 是合成 Q4，需要 Q1/Q2/Q3，
而 2009Q1 那份 10-Q 是純 HTML 沒有 XBRL（SEC 強制申報 2009-06 才對大型申報人上路）。

**但那一期的數字其實拿得到。** 10-Q 的 XBRL 除了當期還帶去年同期的比較欄，
實測 NVDA 2010-05-02 那份 10-Q：

```
cols = ['2010-05-02 (Q2)', '2009-04-26 (Q2)']
                            ↑ 2009 那一季就在第二欄，有 XBRL
```

`_current_q_col()` 只取第一欄，**比較欄整個丟掉**。

### CTH 決策

- 「拿新的一年去對照舊的一年，當作 **fallback**」
- 「看能不能套用到其他狀況」→ 做成**通用的補洞機制**，不要寫死成「只補 pre-XBRL」

### 規格

1. 新增 `_prior_q_cols(df)`：回傳當期以外的所有季度欄（現在 `_current_q_col()` 只回第一個）
2. 三個 `_build_*_table()` 各跑完之後，做一輪 **fallback pass**：
   - 對每個「模板列在某期是 `None`」的洞，找有沒有任何 filing 的比較欄涵蓋該期
   - 有就填進去，並記下這一格是 fallback 來源
3. **優先序鐵則：當期申報優先，比較欄只補洞，永遠不覆蓋已有的值**
   （CTH 確認）。重編（restatement）造成兩版不同時，以當期那版為準
4. 比較欄的欄名同樣**不能採信 `(Qn)`**，要走跟 D0-6 一樣的日期反推
   （`_col_to_quarter_label()` 現在已經是這樣，直接沿用即可）
5. 要不要在 Excel 上標示「這一格是從比較欄補的」——**動手前問 CTH**。
   傾向要標（資料來源不同，分析師應該知道），但會動到版面

### 備案（成本較高，先不做）

SEC Company Facts API（`https://data.sec.gov/api/xbrl/companyfacts/CIK##########.json`），
一次拿回該公司歷來所有 XBRL fact。edgartools 的 `company.get_facts()` 已經在用來
抓流通股數。補洞能力更強，但同一期間會有多個 `frame`／多次重編，取版本的問題更複雜。

### 測試要釘什麼

- 比較欄能補上當期缺的那一期
- **當期有值時，比較欄不覆蓋**（這條最重要，寫成明確的回歸測試）
- 比較欄的 `(Qn)` 標錯時仍算出正確的財季（沿用 D0-6 的日期反推）

### 驗收

重跑 NVDA / INTC / AMD，看 2009–2010 那幾期的缺漏數有沒有下降、2009Q4 能不能合成出來。
**同時要比對「原本有值的期間數字有沒有變」——一格都不該變。**

### 風險：高（改資料來源優先序，會影響所有既有期間）

---

## 尚未決策、不要動的

- **G4** `Other (as reported)` overflow 區天生斷續（concept 改名就長出新列、
  合成 Q4 不補 overflow）。建議先只在 Excel 上標示「原樣呈現、不保證跨期連續」，
  不要做 synonym 合併（合錯就是把兩個不同科目加在一起）
- **G5** Index 完成度評分標準。現行是 9 個關鍵列、只看「最近 4 期全空」才算缺，
  判準極寬且是開發時 AI 自訂的。**動手前要先給 CTH 看「同一份檔案在新舊標準下
  各得幾分」的對照**，再定新標準
